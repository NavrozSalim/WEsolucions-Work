# Deploy scripts

VPS **audit**, **fix**, and **validate** steps, plus Nginx/TLS notes, are documented in one place: **[README.md](../../README.md#vps-deploy-helpers)** in the project root.

Run shell scripts from the `saas-store-sync` directory (where `docker-compose.prod.yml` lives).
