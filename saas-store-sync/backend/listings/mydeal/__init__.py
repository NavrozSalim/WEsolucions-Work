"""MyDeal / Woolworths Marketplace Platform (WMP) Universal API helpers.

Modules:
- ``client`` — HTTP + auth, products, orders, fulfill, cancel, refund
- ``orders`` — sync / normalize / cancel for Orders UI
- ``products`` — publish + price/qty push for managed listings
"""
